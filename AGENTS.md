# Social Workbench 实现阶段指南

用户已在 2026-08-23 明确批准以“小红书/抖音闭环跑通”为目标进入实现阶段，并在 2026-08-25 要求优先接入抖音需求研究。仓库现在包含可选的抖音研究 sidecar、DSH Host staging 插件、发布计划/outbox、对账与反馈事实核心和固定 sidecar 安装器。继续实现时保持分层授权：模型可见插件只生成来源、brief、内容包，并按稳定 ID 读取去身份化研究证据及用户管理的发布/反馈账本；平台登录、搜索/评论采集、媒体下载、本地转写、计划批准、入队、一次性确认、真实执行、对账、反馈采集和复盘接受只保留在用户侧 CLI/UI 边界。

## 当前目标

- 用一套稳定模型描述信息来源、证据、需求信号、内容资产、发布计划和反馈。
- 用官方文档验证每个平台的读取、检索、发布、互动和分析能力。
- 用不可变 plan hash、幂等 outbox、真实 receipt/reconciliation、追加式 metric/feedback 和 review→brief lineage 完成发布学习闭环。
- 评估可复用开源项目，区分直接依赖、外部服务适配和仅供参考的实现。
- 明确隐私、平台条款、版权、授权和人工确认边界。

## 事实规则

- 平台能力必须附官方证据链接、核验日期和置信度；没有证据就是 `unverified`。
- “能在浏览器里操作”不等于“有 API”；“仓库公开”不等于“适合生产或许可兼容”。
- Cookie、私有接口、逆向协议和规避风控不能标记为官方接入。
- 采集只覆盖公开内容、用户自己控制的账号或获得明确授权的数据；不采集私密资料，不构建跨平台身份画像。
- 发布默认停在预览/草稿，真实发布必须绑定具体账号、具体内容和一次性人工批准。
- 密钥未来只能进入 DSH Credentials API；文档、聊天、普通设置、浏览器 payload 和 Git 中不得出现明文凭据。

## 真实平台执行前

首批平台已确定为小红书和抖音，开源依赖已固定版本并完成许可证/误报成功审计，MVP 采用私密测试发布与平台侧反查。账号登录和每次真实发布仍必须由用户确认：实际账号为本人控制、素材有权使用、登录态与诊断证据只保留本机、目标为 `visibility=private`，且每个平台每个 revision 单独签发一次性批准。不能用计划中的 Client、Credentials 注入、反馈采集或真实账号能力冒充当前运行时 facts。
